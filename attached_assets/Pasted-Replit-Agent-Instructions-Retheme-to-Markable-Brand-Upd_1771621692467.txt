Replit Agent Instructions — Retheme to Markable Brand
Update the entire UI color scheme in App.jsx to match Markable's brand.
Replace the current purple/indigo theme with Markable's actual brand system.

Markable Brand Tokens
Replace ALL color references throughout the styles object and inline styles
with these values:
javascript// ── MARKABLE BRAND TOKENS ─────────────────────────────────────────────────
const BRAND = {
  // Core
  bg:           '#0A0A0A',      // near-black background (matches their dark site)
  bgCard:       '#141414',      // card background
  bgCardHover:  '#1C1C1C',      // card hover state
  bgInput:      '#1A1A1A',      // input fields
  border:       '#2A2A2A',      // default border
  borderLight:  '#333333',      // slightly lighter border

  // Brand accent — Markable coral/red
  accent:       '#FF3B3B',      // primary coral red
  accentHover:  '#FF5555',      // hover state
  accentSoft:   'rgba(255,59,59,0.12)',  // soft background tint
  accentBorder: 'rgba(255,59,59,0.3)',   // border tint

  // Text
  textPrimary:  '#FFFFFF',      // primary text
  textSecondary:'#A0A0A0',      // secondary/muted text
  textMuted:    '#555555',      // very muted

  // Semantic colors (keep for signal meaning)
  green:        '#22C55E',      // positive/profit signals
  greenSoft:    'rgba(34,197,94,0.12)',
  orange:       '#F97316',      // warning/test signals
  orangeSoft:   'rgba(249,115,22,0.12)',
  blue:         '#3B82F6',      // info
  blueSoft:     'rgba(59,130,246,0.12)',

  // Gradient — Markable uses clean solid colors, minimal gradients
  // Use this only for primary CTA buttons
  btnPrimary:   '#FF3B3B',
  btnPrimaryHover: '#FF5555',
};

Global style replacements
Make these find-and-replace changes throughout the entire styles object
and all inline styles in the JSX:
Background colors

#0A0A0F → #0A0A0A
rgba(255,255,255,0.04) → #141414
rgba(255,255,255,0.07) → #1C1C1C
rgba(255,255,255,0.03) → #111111

Borders

rgba(255,255,255,0.08) → #2A2A2A
rgba(255,255,255,0.06) → #252525
rgba(255,255,255,0.1) → #333333
rgba(255,255,255,0.12) → #3A3A3A

Primary accent (purple → coral red)

#C084FC → #FF3B3B
#818CF8 → #FF3B3B
rgba(192,132,252, → rgba(255,59,59,
rgba(99,102,241, → rgba(255,59,59,
linear-gradient(135deg, #C084FC, #818CF8) → #FF3B3B

Keep green as-is (revenue/positive signals)

#34D399 stays #34D399
#059669 stays #059669

Keep orange as-is (warning signals)

#FB923C stays #F97316

Text colors

#E8E8F0 → #FFFFFF
#D1D5DB → #E0E0E0
#9CA3AF → #A0A0A0
#6B7280 → #555555
#4B5563 → #404040


Nav bar update
Replace the nav brand section with Markable's actual logo treatment:
javascript// In the nav, replace the brand span content:
<nav style={{
  ...styles.nav,
  background: '#000000',
  borderBottom: '1px solid #222222',
}}>
  <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
    <span style={{
      fontSize: '16px',
      fontWeight: '800',
      color: '#FFFFFF',
      letterSpacing: '-0.3px'
    }}>
      markable
    </span>
    <span style={{
      fontSize: '10px',
      background: '#FF3B3B',
      color: '#fff',
      padding: '3px 8px',
      borderRadius: '4px',
      fontWeight: '700',
      letterSpacing: '0.5px',
      textTransform: 'uppercase'
    }}>
      Creator Intelligence
    </span>
  </div>
  <span style={{ fontSize: '12px', color: '#555555' }}>Demo · Feb 2026</span>
</nav>

Button styles
Replace ALL button gradient styles with these clean Markable-style buttons:
Primary button (was purple gradient)
javascriptbtnPrimary: {
  background: '#FF3B3B',
  color: '#fff',
  border: 'none',
  borderRadius: '8px',
  padding: '12px 22px',
  fontSize: '14px',
  fontWeight: '700',
  cursor: 'pointer',
  letterSpacing: '0.2px',
  transition: 'background 0.15s',
}
// on hover: background '#FF5555'
Secondary/outline button
javascriptbtnOutline: {
  background: 'transparent',
  color: '#FF3B3B',
  border: '1px solid #FF3B3B',
  borderRadius: '8px',
  padding: '10px 18px',
  fontSize: '13px',
  fontWeight: '600',
  cursor: 'pointer',
}
Green button (boost/revenue actions — keep distinct)
javascriptbtnGreen: {
  background: '#22C55E',
  color: '#fff',
  border: 'none',
  borderRadius: '8px',
  padding: '12px 22px',
  fontSize: '14px',
  fontWeight: '700',
  cursor: 'pointer',
}

Insight/callout boxes
Replace the purple insight boxes with Markable's cleaner style:
javascriptinsightBox: {
  background: 'rgba(255,59,59,0.08)',
  border: '1px solid rgba(255,59,59,0.2)',
  borderRadius: '10px',
  padding: '14px 18px',
  marginBottom: '20px',
}
// Label inside: color '#FF3B3B'
// Body text: color '#E0E0E0'

Creator cards on home screen
Each creator card has a colored left border using the creator's own color
(purple, orange, green, blue). Keep those creator-specific accent colors —
they provide visual variety on the home screen. Just update the card
background and border to match the new dark theme.
javascriptcard: {
  background: '#141414',
  border: '1px solid #2A2A2A',
  borderRadius: '14px',
  padding: '24px',
  cursor: 'pointer',
  transition: 'all 0.2s',
}
// hover: background '#1C1C1C', border '#3A3A3A'

Section labels (uppercase headers)
javascriptsectionLabel: {
  fontSize: '11px',
  fontWeight: '700',
  letterSpacing: '1.5px',
  color: '#555555',
  textTransform: 'uppercase',
  marginBottom: '14px',
  marginTop: '28px',
}

Tag/badge pills
javascript// Default tag
tag: {
  display: 'inline-block',
  fontSize: '11px',
  fontWeight: '600',
  padding: '3px 10px',
  borderRadius: '4px',   // Markable uses more square corners than rounded
  marginRight: '6px',
  background: '#222222',
  color: '#A0A0A0',
}

// Accent tag (red)
tagAccent: {
  background: 'rgba(255,59,59,0.12)',
  color: '#FF3B3B',
}

// Green tag (revenue/positive)
tagGreen: {
  background: 'rgba(34,197,94,0.12)',
  color: '#22C55E',
}

Loading spinner
Update spinner color:

border: '3px solid rgba(192,132,252,0.2)' → border: '3px solid rgba(255,59,59,0.15)'
borderTop: '3px solid #C084FC' → borderTop: '3px solid #FF3B3B'


Home screen hero text update
Update the System Insight box copy to match Markable's voice:
javascript// Replace:
"AI GENERATED" label → keep as is

// Update the subtitle on home screen from:
"Select a creator to view their live ad data..."
// To:
"Select a creator to generate AI ad variations, build content calendars, 
and get signal-based boost recommendations."

// Update the insight box label from:
"📊 System Insight"
// To:
"📊 Intelligence Layer"

CSS hover class update
In the <style> tag at the top of each screen, update the hover class:
css.creator-card:hover { 
  border-color: rgba(255,59,59,0.4) !important; 
  transform: translateY(-2px); 
  background: #1C1C1C !important; 
}

.prod-row:hover { 
  border-color: rgba(255,59,59,0.3) !important; 
  background: #1C1C1C !important; 
  cursor: pointer; 
}

Font update
Markable uses a clean geometric sans-serif. Update the Google Fonts import:
javascript// Replace DM Sans with Inter (closer to Markable's typography)
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

// Update fontFamily throughout:
fontFamily: "'Inter', sans-serif"

Summary of visual result
After these changes the demo will look like:

Pure black/near-black backgrounds matching Markable's dark site
Coral red (#FF3B3B) as the primary accent replacing all purple
Clean square-ish pill badges instead of fully rounded
Inter font matching Markable's clean geometric style
Green retained for all revenue/profit signals (intentional contrast)
Creator-specific accent colors retained on home screen cards
Nav bar matches Markable's minimal black header treatment
Buttons solid color, no gradients — cleaner and more on-brand